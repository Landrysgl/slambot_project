twist_mux:
  ros__parameters:
    use_stamped: false
    topics:
      navigation:
        topic   : cmd_vel
        timeout : 0.5
        priority: 10
      joystick:
        topic   : cmd_vel1
        timeout : 0.5
        priority: 50
      # joystick2:
      #   topic   : cmd_vel2
      #   timeout : 0.5
      #   priority: 100